---
name: node
description: >-
  Runs Node.js and npm via the `node;` shell alias (fnm version select). Use when
  running npm, node, npx, or npm scripts in the agent terminal, or when node is
  not found in PATH.
---

# Node (fnm)
`node;` = alias → fnm version select. Ensures node available in agent terminal.
Use: `node; npm [script]`
